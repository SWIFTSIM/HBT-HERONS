# Overview

In this section we provide several example scripts of how to interface with the data generated by HBT-HERONS. We also use the examples we provide to illustrate some of the less-trivial concepts within HBT-HERONS.

* [Population-level properties](./population_level_statistics.md): bound mass and $V_{\rm max}$ functions, selection of specific populations and merger rates.
* [Internal halo population](): spatial distribution of satellites, subhalo hierarchy.
* [Merger trees](./merger_trees.md): following the main progenitor branch and obtaining all sink and disrupted progenitors.

We have written the code so that you can apply it to any HBT-HERONS catalogue, including your own. However, if you do not have any such catalogue available yet, we provide an example that you can download following the steps below.

## Example catalogue

We have generated an example HBT-HERONS catalogue from a dark-matter-only simulation of a $(25\,\mathrm{Mpc}^{3})$ volume with a particle mass of $m_{\rm p} = 10^{9}\,\mathrm{M}_{\odot}$. Outputs are logaritmically-spaced in expansion factor, and 64 of them are saved from redshift $z = 10$ to $z = 0$.

To download, run the following:

```bash
tar -xzvf sorted_catalogues.tar.gz
```

Once unzipped, the file structure will be as follows.


```bash
RawCatalogues/{SnapNumber:03d}/SubSnap_{SnapNumber:03d}.{MPI_rank}.hdf5
SortedCatalogues/OrderedSubSnap_{SnapNumber:03d}.hdf5
```
where `SnapNumber` goes from `000` to `063` (64 snapshots analysed) and `MPI_rank` from `0` to `3` (4 MPI ranks used during the analysis).

The two directories, `RawCatalogues` and `SortedCatalogues`, are two different formats in which to:

 The files in `RawCatalogues` are the outputs of HBT-HERONS, except for the field, which has been removed from.
